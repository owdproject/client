{
  "name": "@owdproject/client",
  "private": true,
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate --dev",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@owdproject/app-about": "^0.1.4",
    "@owdproject/core": "^3.4.0",
    "@owdproject/theme-nova": "^0.0.4",
    "h3": "^2.0.1-rc.22"
  }
}
