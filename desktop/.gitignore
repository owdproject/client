# Dependencies
node_modules

# Open Web Desktop (force a clean slate for new clones)
desktop.config.ts
package.json

# Nuxt (desktop app root)
.output
.data
.nuxt
.nitro
.cache
dist
*.tsbuildinfo

# Logs
logs
*.log

# Local env
.env
.env.*
!.env.example

# IDE / OS
.idea
.vscode/*
!.vscode/extensions.json
.DS_Store
._*

# TLS / local certs (see root .gitignore /desktop/certs)
certs

# Test zip file with module-fs
public/test.zip

# Generated during ATProto OAuth setup (desktop.config writeClientMetadata)
public/client-metadata.json
